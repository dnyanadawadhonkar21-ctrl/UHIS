import React from "react";
import { useToast } from "../../context/ToastContext";

export default function ToastContainer() {
  return null; // The ToastProvider already renders the toasts container globally
}
